#Thiago
#Atividade sugerida pelo profesor wandre

#Calcule o valor de X

x <- c("a", "a", "b", "b", "b", "c" )
table(x)
2*x*2-x-4=0
(-b + sqrt(b^2 - 4*a*c))/(2*a)
a <-2; b <- -1; c <- -4
(-b + sqrt(b^2 - 4*a*c))/(2*a)
(-b - sqrt(b^2 - 4*a*c))/(2*a)


#x=2+